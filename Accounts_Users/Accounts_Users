cat /etc/passwd

cat /etc/group

id user_name

grep -i user_name /etc/passwd

who --- просмотр пользователей которые вошли в систему.

w --- Просмотр какие пользователи активны и откуда подключились.

last --- Отображает информацию о последнем входе пользователей.

su - 

su -c "whoami"

cat /etc/sudoers

visudo

sudo useradd user_name --- Добавить пользователя

grep -i user_name /etc/passwd

grep -i user_name /etc/shadow

sudo passwd user_name --- установить пароль для пользователя

passwd --- Сменить паспорт пользователя под которым сейчас в системе

id user_name --- Просмотреть данные по пользователю

userdel user_name --- Удаление пользователя

groupadd -g 1015 devops --- Создание группы

groupdel devops --- Удаление группы

ACCESS CONTROL FILES

sudo vipw

sudo vipw -s

sudo vigr

sudo vigr -s

grep -i ^user_name /etc/passwd

/etc/passwd --- Содержит в текстовом формате список пользовательских учетных записей

grep -i ^user_name /etc/shadow 

/etc/shadow --- Содержит информацию о паролях пользователей системы

grep -i ^max /etc/group

/etc/group --- Содержит имена присутствующих групп и списки членов каждой группы






























 























